/*
import 'package:flutter/widgets.dart';

abstract class Only {
  static BorderRadius bottomRight15AndBottomLeft15({
    required BuildContext context,
  }) {
    double height = MediaQuery.sizeOf(context).height;
    return BorderRadius.only(
      bottomLeft: Radius.circular(height * 0.021),
      bottomRight: Radius.circular(height * 0.021),
    );
  }

  static BorderRadius topRight15AndTopLeft15({required BuildContext context}) {
    double height = MediaQuery.sizeOf(context).height;
    return BorderRadius.only(
      topLeft: Radius.circular(height * 0.021),
      topRight: Radius.circular(height * 0.021),
    );
  }
}
*/
