/*
import 'package:flutter/material.dart';
import 'package:misk/core/utils/styles/style_to_texts.dart';

class CustomTextNormal18DisplayHadithView extends StatelessWidget {
  const CustomTextNormal18DisplayHadithView({
    super.key,
    required this.text,
    this.color,
  });
  final String text;
  final Color? color;
  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textDirection: TextDirection.rtl,
      style: StyleToTexts.textStyleNormal18(
        context: context,
      ).copyWith(color: color),
    );
  }
}
*/
