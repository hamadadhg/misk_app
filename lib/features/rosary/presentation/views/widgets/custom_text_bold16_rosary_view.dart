/*
import 'package:flutter/material.dart';
import 'package:misk/core/utils/styles/style_to_colors.dart';
import 'package:misk/core/utils/styles/style_to_texts.dart';

class CustomTextBold16RosaryView extends StatelessWidget {
  const CustomTextBold16RosaryView({super.key, required this.selectedValue});
  final String selectedValue;
  @override
  Widget build(BuildContext context) {
    return Text(
      selectedValue,
      style: StyleToTexts.textStyleBold16(
        context: context,
      ).copyWith(color: StyleToColors.blackColor),
    );
  }
}
*/
