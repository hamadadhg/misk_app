/*
import 'package:flutter/material.dart';
import 'package:misk/core/utils/styles/style_to_texts.dart';

class CustomContainCardMoreIslamicBenefitHomeView extends StatelessWidget {
  const CustomContainCardMoreIslamicBenefitHomeView({
    super.key,
    required this.image,
    required this.text,
  });
  final Image image;
  final String text;
  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        image,
        Text(text, style: StyleToTexts.textStyleSemiBold14(context: context)),
      ],
    );
  }
}
*/
