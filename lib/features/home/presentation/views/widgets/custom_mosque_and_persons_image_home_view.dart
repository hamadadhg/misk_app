/*
import 'package:flutter/material.dart';
import 'package:misk/gen/assets.gen.dart';

class CustomMosqueAndPersonsImageHomeView extends StatelessWidget {
  const CustomMosqueAndPersonsImageHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Positioned.fill(
      child: LayoutBuilder(
        builder: (context, constraints) {
          return Align(
            alignment: Alignment.bottomCenter,
            child: Assets.images.mosqueAndPersonsImage.image(
              width: constraints.maxWidth,
              fit: BoxFit.fill,
            ),
          );
        },
      ),
    );
  }
}
*/
